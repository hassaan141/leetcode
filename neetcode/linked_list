class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode* back = head;
        ListNode* front = head->next;

        while(front != nullptr){
            back->next = 
        }
    }
};
